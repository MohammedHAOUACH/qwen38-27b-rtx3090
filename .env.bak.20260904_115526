SPEC=dflash2
PREFIX_CACHE=1
EXTRA_ARGS="--tensor-parallel-size 2"
CTX=huge

# Qwen3.8 native context is 262144 (text_config.max_position_embeddings).
# Without this, start_qwen.sh silently drops MAX_LEN to its dflash2+huge default (245760).
DFLASH_MAX_LEN=262144
